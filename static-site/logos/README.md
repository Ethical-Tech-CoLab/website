# Client & partner logos

Drop a logo file here and it appears automatically on the About page — no code
changes needed. The site looks for `/logos/<slug>.<ext>` where `<slug>` is the
org name lowercased with non-letters turned into dashes. If no file is found it
falls back to a lettered monogram.

Supported extensions (checked in this order): `.svg`, `.png`, `.webp`, `.jpg`.
Square images (or with transparent padding) look best; they render on a white
rounded tile.

## Expected filenames

Clients:
- `generative-ai-for-good.png` — Generative AI for Good
- `d-id.png` — D_ID
- `tradeverifyd.svg` — Tradeverifyd (already added; the dark wordmark from
  tradeverifyd.com, which reads correctly on the white logo tile. Formerly
  listed here as Mesur.io, before that org rebranded in 2025.)
- `rivr.png` — Rivr
- `human-generated-by-blockapps.png` — Human Generated by BlockApps

SupplyTrace was removed from the site in 2026-08, so no logo is needed.

Founding partners:
- `microsoft.svg` — Microsoft (already added)
- NYU SPS + CGA use `/nyu-sps-cga-logo.jpg` (already added)

Partners:
- `100x.png` — 100x
- `apne-aap-women-worldwide.png` — Apne Aap Women Worldwide
- `gaia.png` — Gaia
- `tavily.svg` — Tavily (already added; the square mark from tavily.com)
- `b3iq.png` — B3IQ (already added; the horizontal wordmark from b3iq.org. It
  is white-only, so its card sets `logoTile: "dark"` — see `PartnerOrg`.)
- `microsoft-garage.png` — The Microsoft Garage (already added; the site logo
  from microsoft.com/en-us/garage)

Grid Bank was removed from the site in 2026-08. `grid-bank.jpg` is kept here but
is no longer referenced; delete it if the organisation is not coming back.

To use a different name for any org, set its `logo` field in
`src/content/site.ts` to the exact path (e.g. `logo: "/logos/blockapps.png"`).
